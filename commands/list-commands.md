---
description: List all custom slash commands and personal skills (plugins/built-ins via /help)
allowed-tools: Bash(bash:*)
---

Here are the available custom commands and skills on this machine:

!`bash ~/.claude/scripts/list-commands.sh`

Relay the listing above to the user as-is, preserving the section grouping (personal commands, project commands, personal skills, and the `/help` pointer for plugin/built-in commands). Keep formatting tidy and add no commentary beyond a single-line header.
